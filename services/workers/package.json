{
  "name": "workers",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "clean": "rimraf .wrangler"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260626.1",
    "rimraf": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
