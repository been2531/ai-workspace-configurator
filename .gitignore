node_modules/
dist/
out/
.turbo/
*.vsix
.env
.env.local
.wrangler/

# Firebase 서비스 계정 키 — 절대 커밋 금지
*.json.secret
*firebase-adminsdk*.json
service-account.json

# scripts/ 는 pnpm workspace 외부 — lock 파일 / node_modules 는 로컬 전용
scripts/package-lock.json
scripts/node_modules/

# 로컬 작업 메모 — git에 올리지 않음
TASKS.md
