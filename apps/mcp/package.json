{
  "name": "@ai-workspace-configurator/mcp",
  "version": "0.1.0",
  "description": "AI Workspace Configurator MCP Server — Claude Code 네이티브 통합",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/been2531/ai-workspace-configurator.git",
    "directory": "apps/mcp"
  },
  "keywords": ["mcp", "claude-code", "ai-workspace", "model-context-protocol"],
  "bin": {
    "ai-workspace-mcp": "./dist/index.js"
  },
  "type": "commonjs",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@ai-workspace-configurator/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
