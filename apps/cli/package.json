{
  "name": "@ai-workspace-configurator/cli",
  "version": "0.1.0",
  "description": "AI Workspace Configurator CLI — CLAUDE.md / AGENTS.md / .cursorrules 자동 생성",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/been2531/ai-workspace-configurator.git",
    "directory": "apps/cli"
  },
  "homepage": "https://github.com/been2531/ai-workspace-configurator#readme",
  "keywords": ["claude-code", "ai", "codex", "cursor", "mcp", "workspace", "generator"],
  "bin": {
    "ai-workspace": "./dist/index.js"
  },
  "type": "commonjs",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@ai-workspace-configurator/core": "workspace:*",
    "@clack/prompts": "^0.7.0",
    "commander": "^12.0.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
